"""local-webradio — une station de radio personnelle.

Le découpage est décrit dans ARCHITECTURE.md §1 : `core` décide, `adapters`
parlent au monde extérieur, `app` câble les deux une seule fois.
"""
